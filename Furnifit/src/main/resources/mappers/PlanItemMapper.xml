<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
                        
<mapper namespace="com.furnifit.planitem.dao.PlanItemDao">

	<!-- 회원별 배치도 항목 리스트 -->
	<select id="listAll" resultType="PlanItem" parameterType="String">
	
		SELECT i.planitem_id AS "planitemId",
		       i.plan_id AS "planId",
		       i.width,
		       i.height,
		       i.length,
		       i.acreage,
		       i.image,
		       i.name	
		FROM   planitems i JOIN plans p
		ON     i.plan_id = p.plan_id
		WHERE  p.email = #{email}
		
	</select>
	
	<!-- 배치도 삭제 -->
	<delete id="delete" parameterType="map">
		DELETE FROM planitems
		WHERE  planitem_id = #{planitemId}
		AND    plan_id = #{planId}
	</delete>
	
</mapper>